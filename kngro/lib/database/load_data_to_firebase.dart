class update_firebase_data{

  Future<void> loadUserDataToFirebase() async {
    // await FirebaseFirestore.instance.collection('users').get().then((value) {
    //   value.docs.forEach((element) {
    //     FirebaseFirestore.instance.collection('users').doc(element.id).set(element.data());
    //   });
    // });
  }
}